<UserControl x:Class="Steady_Management_App.Views.DepartmentFormUcView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Nombre del Departamento:" Grid.Row="0" VerticalAlignment="Center" />

        <TextBox Text="{Binding DepartmentName, Mode=TwoWay}"
                 Width="250"
                 Grid.Row="1"
                 Margin="0,10,0,10" />

        <Button Content="Guardar"
                Command="{Binding SaveCommand}"
                Width="70"
                Grid.Row="2"
                HorizontalAlignment="Left" Margin="0,0,0,174" />
    </Grid>
</UserControl>
