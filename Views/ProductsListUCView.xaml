<UserControl x:Class="Steady_Management_App.Views.ProductListUCView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Steady_Management_App.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">



    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Barra de botones -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Agregar" Click="AddProduct_Click" Margin="5"/>
            <Button Content=" Editar "
        Click="EditProduct_Click" Margin="5"
        IsEnabled="{Binding SelectedItem, Converter={StaticResource NullToBool}, ElementName=ProductsDataGrid}" Height="27"/>

            <Button Content="Eliminar"
        Click="DeleteProduct_Click"
        IsEnabled="{Binding SelectedItem,
                    ElementName=ProductsDataGrid,
                    Converter={StaticResource NullToBool}}" Height="27"/>

        </StackPanel>

        <!-- Tabla de productos -->
        <DataGrid x:Name="ProductsDataGrid"
              Grid.Row="1"
              AutoGenerateColumns="False"
              CanUserAddRows="False"
              IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"        Binding="{Binding ProductId}"    Width="75"/>
                <DataGridTextColumn Header="Nombre"    Binding="{Binding ProductName}"  Width="*"/>
                <DataGridTextColumn Header="Precio"    Binding="{Binding Price}"        Width="100"/>
                <DataGridTextColumn Header="Categoría" Binding="{Binding CategoryName}" Width="200"/>
                <DataGridTextColumn Header="¿Tiene impuestos?" Binding="{Binding IsTaxable}" Width="110"/>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
