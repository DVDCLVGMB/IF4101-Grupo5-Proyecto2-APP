<UserControl x:Class="Steady_Management_App.ClientsListUcView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:Steady_Management_App.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">


    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Agregar" Click="Agregar_Click" Margin="5"/>
            <Button Content="Editar" Click="Editar_Click" Margin="5"/>

            <Button Content="Eliminar" Command="{Binding DeleteClientCommand}" Margin="5"/>


        </StackPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Clients}"
                  SelectedItem="{Binding SelectedClient}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ClientId}" Width="Auto"/>
                <DataGridTextColumn Header="Nombre Empresa" Binding="{Binding CompanyName}" Width="*"/>
                <DataGridTextColumn Header="Nombre Contacto" Binding="{Binding ContactName}" Width="*"/>
                <DataGridTextColumn Header="Apellido" Binding="{Binding ContactSurname}" Width="*"/>
                <DataGridTextColumn Header="Teléfono" Binding="{Binding ClientPhoneNumber}" Width="*"/>
                <DataGridTextColumn Header="Dirección" Binding="{Binding ClientAddress}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
